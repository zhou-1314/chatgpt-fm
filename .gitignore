# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local runtime config
.env

# 音频成品太大，不进 git；可由解读稿用 edge-tts 重新合成
*.mp3

# autorun 运行日志
content/autorun.log

.DS_Store

# 上传套件（本地工作目录，可重新生成）
content/upload/

# 生成的 RSS（部署到服务器，不入库）
content/feed.xml

# 部署手册（含服务器信息，本地保留不上传）
DEPLOY.md

# 一键周更脚本（含服务器信息，本地保留）
weekly.sh
